# Base URL of the Meridian server the console talks to. Client-side only:
# this value is inlined into the browser bundle (NEXT_PUBLIC_*), so it must
# be an address the user's browser can reach. No server-side secrets live here.
NEXT_PUBLIC_MERIDIAN_URL=http://localhost:8181
